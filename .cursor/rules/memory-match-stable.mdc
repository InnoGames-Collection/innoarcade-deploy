---
description: Memory Match is stable v1 — do not modify without explicit user request
globs: src/games/memory-match/**,games/memory-match/**
alwaysApply: false
---

# Memory Match — stable v1

`memory-match` is tagged `stable: 'v1'` in `src/platform/catalog.ts`. See `STABLE.md`.

## Do not change

Unless the user **explicitly** asks to modify Memory Match:

- Do not edit files under `src/games/memory-match/` or `games/memory-match/`
- Do not change scoring formula, card grid, tournament UX, or hub-themed styling

## Instead

- Copy Memory Match **patterns** into shared modules or other games (e.g. `fruit-slice`)
- Fix platform/hub/backend issues without touching Memory Match game code
- If a shared change affects Memory Match, **tell the user** which shared file changed and why
