---
description: Memory Match is stable v1 — do not modify without explicit user request
globs: src/games/memory-match/**,games/memory-match/**,assets/memory-match/**,scripts/*memory-match*
alwaysApply: false
---

# Memory Match — stable v1

`memory-match` is tagged `stable: 'v1'` in `src/platform/catalog.ts`. See `STABLE.md`.

## Do not change

Unless the user **explicitly** asks to modify Memory Match:

- Do not edit files under `src/games/memory-match/`, `games/memory-match/`, or `assets/memory-match/`
- Do not edit `scripts/*memory-match*` icon/process scripts
- Do not change scoring formula, card grid, tournament UX, icons, or game-specific UI

## Instead

- Copy Memory Match **patterns** into shared modules or other games
- Fix platform/hub/backend issues without touching Memory Match game code
- If a shared change affects Memory Match, **tell the user** which shared file changed and why
