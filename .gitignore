node_modules
dist
*.local
.env
.env.*
!.env.example
.DS_Store
.vite
# NOTE: public/ holds deployable static assets (game cover images + the vendored
# LexiQuest app). It MUST be committed or Vercel builds without them (404s).
